{
  "name": "obsidian-vault-mcp-plugin",
  "version": "0.18.2",
  "description": "Obsidian plugin embedding an in-renderer MCP server with direct app.* access, reached via Unix socket + bundled stdio bridge.",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "tsc --noEmit && node --import tsx --test 'tests/*.test.mjs'",
    "pretest": "npm --prefix ../core run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@vault-mcp/core": "*",
    "isomorphic-git": "^1.41.7",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.25.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.6.6",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
