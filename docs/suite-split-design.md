# The suite split — core host, governance provider, satellite modules

> [!note] ADOPTED by Nelson 2026-08-25 ("looks good", after reading in full) — S2/S3 gated on an independent fresh-context perimeter review (lane consolidated 2026-08-25; governor-lead retired)
> §10's decisions: (1) repo name `obsidian-mcp-suite` — decided; (2) adoption — given; (3) satellite release posture — per recommendation (private-tier satellites unreleased until independently needed) absent a contrary ruling. The register updates in §8 are Nelson's rulings, recorded by this adoption. The S1 package may begin; S2/S3 (the seam and the split) additionally require an independent fresh-context perimeter review of §3/§5 (the reviewer-separation discipline survives the lane consolidation of 2026-08-25, under which governor-lead retired and the release + perimeter-review lanes consolidated onto the sole remaining agent). [Status and compatibility](status-and-compatibility.md#current-release-state) remains the single owner of shipped truth throughout the split.

## 0. Threat model — who this protects against, and who it does not

**Ruled by Nelson, 2026-08-27, after asking the right question of every perimeter finding: what has to be true in the real world for this to happen?**

**What the suite protects against: MISTAKES.** Agents write the wrong thing, write too much, or write confidently while wrong. A human wants to see changes before they count, and an honest record of what happened. That is the product need, and it is met by proposals, the Admit gesture, the journal, and the review pane — none of which is anti-adversary machinery.

**What it does NOT protect against, deliberately: hostile code already running inside Obsidian.** Every forged-proposal / laundered-session class of attack shares one precondition — arbitrary JavaScript executing in the renderer (a compromised community plugin, a bad update, a hostile js-engine/DataviewJS/execute-code block). At that point the attacker can already read the whole vault and write anything through `app.vault`. The marginal thing forging buys is making a bad change look APPROVED, which matters only where the audit trail is evidence against an adversary. In a single-operator vault running its own agents, that is insurance against something that is not happening.

**What would change the answer:** shipping to users who install untrusted plugins beside this one, or any deployment where the standing chain is evidence in a dispute. **The full adversarial design — the nine perimeter conditions in their strong form — is preserved verbatim on the branch `reference/hostile-threat-model` (at `921f72e`)**, so that decision can be reversed by reading rather than re-deriving.

**The filter this licenses:** a perimeter condition survives into the build if it is (a) cheap, (b) a correctness fix independent of malice, or (c) protection against an agent MISTAKE. It defers to the reference branch if a hostile local actor is its only justification.

## 1. Why

The consolidation era served its purpose: scattered work across many repos became one codebase with one test suite, one review discipline, and one perimeter. It also produced a plugin that is arcane and amorphous — an MCP host, a write kernel, a governance system, and roughly a dozen capability modules in one bundle, where the boundaries the decision register already ruled (D07's public-core / public-optional / private-operator tiers) exist only as settings flags.

The direction (Nelson, 2026-08-25): separate the codebase into a suite — the MCP host as the core, other modules as plugins that publish their tools to it, and **governance itself as an optional provider that plugs in**. The publishing mechanism for this already ships (`vault-mcp-api`); this document designs the rest.

## 2. The target shape

```text
one monorepo (renamed; see §4)
├── packages/core            @vault-mcp/core — shared guard/recognizer lib (exists)
├── packages/vault-mcp-api   the publishing SDK, npm: vault-mcp-api (exists)
├── packages/host            THE CORE plugin, id `vault-mcp`
│     transport (socket+bridge), guard (read-only, allowlist), kernel v0
│     (queue, journal, idempotency, locks, uid index, record guard),
│     operation executor + action registry, core obsidian tools,
│     module host, external-tool registry, THE GOVERNANCE SEAM (§5)
├── packages/governor        THE GOVERNANCE PROVIDER plugin, id `governor`
│     everything under kernel/governance + src/governance today:
│     proposals, verification, admission, cohorts, mandates,
│     transformations/promotion, history store, sessions, observations,
│     migration/cutover/binding, the review pane, the gesture perimeter
└── packages/<satellite>…    one plugin per extracted module (§6)
```

Three roles, three trust postures:

- **The host** is the honest standalone product: audited, journaled, allowlist-scoped vault access over MCP. Safe by default with nothing else installed.
- **The governance provider** is the flagship consumer of the seam. All authority — the pane, the gesture gate, the standing chain, mandates, promotion — is born and dies inside it, closure-held, exactly as today. Installing it turns "audited access" into "governed access".
- **Satellites** are capability packs (JD scheme, vocabulary, skills compiler, …) publishing tools through `vault-mcp-api` like any third-party plugin, with no special standing.

## 3. Why the optional governance provider is doctrine-clean

The §9 reachability invariant says accept-class capabilities must never be reachable from `app`. The split survives it because **authority never crosses the seam** — only two classes of thing do, and each is safe in its direction:

- **Candidates flow outward.** The host tells the provider "a write completed; here are the exact base and proposed bytes." The provider turns that into a proposal. A proposal confers nothing (the #221 axis: agents and machinery supply candidates; humans decide).
- **Vetoes flow inward.** The provider registers refuse-this-write checks (the accept-forbidden transition guard, the legacy-writer guard). A veto can only make the host refuse MORE. Fail closed by construction.

The seam's **registration function** is necessarily reachable from `app` — registration happens through the host plugin's API object, and that is harmless. **The registered CLOSURES must not be** (independent perimeter review, 2026-08-27, finding 1): the §3 argument above proves the safety of the two hook CLASSES and does not, by itself, prove the safety of the hook REGISTRY. A reachable observer is the thing that manufactures proposals — call it directly with forged facts and you get an internally-consistent proposal in the automatic-admission path, which is strictly more than the `app.vault` equivalence covers (writing bytes through `app.vault` produces a loud queue entry; a forged admission SILENCES the queue). The accept perimeter already owns the correct pattern — `governor/wiring/wiring.ts` holds its guards and stores in module-private WeakMaps keyed by the plugin instance — and the seam's hook lists MUST use it. (Same review: `main.ts`'s `private externalRegistry` is compile-time-private only, so third-party handlers are reachable from `app` today; fixing that rides with S2.)

What the seam must NEVER offer is the third hook class: anything that can **mutate a write in flight or assert acceptance**. Observe or refuse — nothing else. Rule 6 (§5) generalizes it: the seam accepts registrations that can only ADD A REFUSAL, never a predicate the host consults for permission.

## 4. Names and identities

- **The monorepo** renames away from `obsidian-governor` — "governor" now means the governance provider, not the whole. **Chosen: `obsidian-mcp-suite`** (Nelson, 2026-08-25). Verified clean at choosing time: no GitHub repo of that name anywhere, npm free, no community-registry collision — and `-suite` is an established plugin-family naming pattern there. (`obsidian-vault-mcp` was considered and rejected: three same-name neighbors including a community-directory plugin repo, plus an existing "Vault MCP" org brand in the exact niche.)
- **The host plugin id** returns to **`vault-mcp`**. The 0.12.0 id-migration machinery (`id-migration.ts`, `migrateLegacyModuleIds`) exists and runs the reverse direction with the same care: folder migration, settings adoption, discovery grace period, bridge dual-id lookup (the SDK already resolves both ids).
- **The governance provider plugin id** is **`governor`** — the name finally names the thing.
- **npm names do not change** (`vault-mcp-api`, `@vault-mcp/core`): published contracts keep their identities, per the standing naming rulings.
- **Live-vault cost, stated:** one id migration on the operator's install (folder + settings + re-register), using shipped machinery. The governance state (`governance/` dir, cutover marker, store binding, history dir slug) must migrate with its OWNING plugin — the migration plan in the S3 package names every file and where it lands, and the chain backup covers the transition.

## 5. The seam — the host's governance hook API

An `apiVersion: 2` extension of the existing external-tool API. Exact shape (names bikesheddable; classes are not):

```ts
interface WriteFacts {
  path: string;
  baseBytes: Uint8Array | null;      // null = creation
  proposedBytes: Uint8Array;
  operation: { id: string; action: string; actionVersion: number; sessionId: string | null };
  actor: JournalActor;                // the journal's own attribution, verbatim
}

interface GovernanceSeam {
  /** Pre-write, refuse-only. A typed refusal aborts the write; null lets it pass. Throwing = refusal (fail closed). */
  registerWriteVeto(id: string, veto: (facts: WriteFacts) => { code: string; detail: string } | null): void;
  /** Post-write, observe-only. Return value ignored; throwing is logged and swallowed — an observer must never cost the caller their result. */
  registerWriteObserver(id: string, observe: (facts: WriteFacts) => void | Promise<void>): void;
  /** Session liveness consultation (the provider owns sessions/mandates; the host asks). Absent provider = no constraint. */
  registerSessionGate?(id: string, gate: (sessionId: string) => Promise<{ live: boolean; detail?: string }>): void;
}
```

Rules, each to be pinned:

1. **No mutation hooks.** The seam's TypeScript surface has no method whose return value the host writes; a source scan pins that the host never reads a hook's return except the veto's refusal.
2. **Vetoes fail closed; observers fail open.** A throwing veto refuses; a throwing observer logs once and the write stands (facts about a write must not cost the write — the promotion-recorder discipline).
3. **Observers run AFTER the journal record** — nothing the provider does can precede or suppress the host's own audit.
4. **The host works with zero hooks registered.** Every seam consultation is `for (const hook of hooks)` over a possibly-empty list — the standalone host is the vacuous case, not a special one.
5. **What moves across is bytes and identifiers, never capabilities.** `WriteFacts` carries no callback, no store handle, no app reference beyond what any plugin already has.

What the governance provider registers through it: the accept-forbidden transition veto, the legacy-writer veto (pre-cutover), the record of write facts that today rides the in-tree `propose` hook (WP6b) — becoming an observer that produces proposals inside the provider. The producer's mandate stamping, budget charging, and everything downstream are provider-internal and unchanged.

### Amendments from the independent perimeter review (2026-08-27) — S2's acceptance criteria

The review returned **APPROVED WITH CONDITIONS**: the direction and the two-class framing are sound, but §5 as first written is not buildable as perimeter without these. Each is testable, and S2 ships only with its pins.

**Shape changes to the API above:**

- **`registerSessionGate` is refusal-shaped, not boolean.** As specced it returned `{live: boolean}` — an ANSWER, and `live: true` is an ALLOW, so a hostile registrant could un-refuse a revoked session and launder a write through the host's own guarded, journaled path. It now returns `{code, detail} | null` exactly like the veto, so the type cannot express an allow. (This is why rule 6 exists: `setLegacyWriteGuard` is a permission-shaped predicate today and is safe ONLY because it is unreachable from `app`; moving that shape onto an app-walkable API would destroy the protection that makes it safe.)
- **Every `register*` returns a disposer, and the disposer is the ONLY revocation.** §5 had no unregistration story at all, which cannot ship (plugins unload constantly; a stale veto holding a dead provider's stores is worse than none). No id-addressed `unregisterHook(id)` — the existing `unregisterTools(ownerPluginId)` is forgeable by any caller today and must be fixed the same way. The `id` argument stays as diagnostic labelling, not an address.
- **`WriteFacts` is a per-hook defensive copy** — bytes copied, `operation`/`actor` deep-frozen. Rule 1 pinned only that the host ignores return VALUES; argument aliasing was the unclosed half (a veto handed the live buffer can mutate the bytes about to be written, or forge `actor.session` into the host's own journal).

**The nine conditions, retriaged against §0 (Nelson, 2026-08-27).** Six survive as cheap, correctness, or mistake-protection; three defer to `reference/hostile-threat-model`, where all nine keep their strong form.

**BUILD in S2:**

1. **[cheap]** Registered hooks unreachable from `app` (module-private WeakMaps), pinned by a runtime transitive walk plus a source tripwire; `main.ts`'s `externalRegistry` fixed in the same motion.
2. **[better design, not armor — a hook that can only say no is simpler to reason about]** No permission-shaped hook (rule 6), pinned as a source scan over the seam's declared return types; if any boolean survives, deny-wins unanimity with two-disagreeing-registrants tests.
3. **[CORRECTNESS — a missing disposer leaves stale callbacks on every plugin reload, and the forgeable `unregisterTools` lets any script unhook another plugin's tools, QuickAdd's included]** Disposer-only revocation, pinned: a caller holding the api object cannot revoke a hook it did not register.
4. Defensive-copy `WriteFacts`, pinned three ways (byte mutation, actor mutation, per-hook independence).
5. **[correctness]** Placement and hang budgets: vetoes and the gate run INSIDE the kernel's queued closure where `sessionLive` runs today, so `WRITE_TIMEOUT_MS` bounds them (a hanging veto is abandoned and the write refused — no new timeout machinery needed); observers are NOT awaited on the caller's result path (a hanging observer must not cost a write that already landed).
6. **[MISTAKE-protection — the condition that most earns its keep under §0]** Post-split, `governor` is just another plugin id, so `obsidian_plugin_toggle('governor', false)` becomes a one-call way for AN AGENT DOING CLEANUP to switch off the thing that reviews it. **Simplified build form:** the plugin-toggle and uninstall tools refuse a registered provider's id — the rule `tools-nav.ts:471` already applies to the host's own id. (The full latch, with persisted first-registration state and mutating tools refusing while a latched provider is absent, defends against DELIBERATE removal and lives on the reference branch.) Pin: the refusal, and that it names the provider.
7. **[RULED — Nelson, 2026-08-27: the HOST keeps minting]** Session ownership is settled: the host mints a session per connection exactly as it does today (`server.ts:131` — `openSession`, with the `scopeDigest` over that connection's read-only + allowlist state, closed on `Protocol.onclose`), because a session is transport state and the host is the only thing that knows a connection began. **`SessionV1` therefore becomes a published contract** (`@vault-mcp/core`), not provider-internal — §6's row moves accordingly. The provider's role is REFUSAL ONLY: it keeps its own revocation state and answers the seam's refusal-shaped session hook, so a human revoking a session in the review pane reaches the write path as a refusal at dequeue. The host never asks the provider for permission and never learns WHY a session was refused beyond the refusal's own detail string. Consequence to build: session-revocation UI and its durable record live in the provider; the host's session record stays what it is today (identifiers, scope digest, lifecycle), and the seam carries no connection-lifecycle notification — the provider does not need one, because it never mints.
8. Ship `registerWriteVeto` only with a real registrant and a round-trip test — §5/§6's two named registrants were both wrong on the code (the fuller transition guard is `acceptTransitionReason`, host-side in `@vault-mcp/core`; the legacy-writer guard is a provider-internal `BaselineStore` predicate that never touches the seam). At a perimeter, YAGNI is a safety property.
9. **[layering]** Layering: move `wiring/mount-state.ts` host-side (the host uses it for the scheme panes too); split the generic disposition-descriptor shape into `@vault-mcp/core` (the provider currently type-imports a future SATELLITE); publish `canonical-json` and `digest` as contracts; and rule on observations (host-side capture with territories as host config, or a capture hook).

   **[RULED at S3, 2026-08-27 — host-side capture, and territories is a CONTRACT, not host config.]** Neither option as drafted survived contact with the code, so the ruling is three-part:

   - **The observation blob store goes to the HOST.** `governor/wiring/observations/local-store.ts` has exactly one consumer — `mcp/server.ts` — and it already imports host machinery to do its job (`kernel/observations/store.js`, `paths.js`). It was misfiled under `governor/`, not governance. Capture records what the host's own transport read, which is the same class of fact as the session scope digest the host already asserts about a connection. **No capture hook is built:** the seam gains no surface for a registrant that does not exist, per condition 8's YAGNI-at-a-perimeter principle.
   - **The provider keeps the observation REFERENCE, and never sees the store.** `governor/kernel/contracts/subject-v1.ts` and the admission/mandate policies reason over `{id, digest, capture}` triples — durability, ephemerality, what may support a proposal (D16). That is contract-level reasoning and stays provider-side. Confirmed by scan: no provider module imports the blob store.
   - **Guarded territories is published into `@vault-mcp/core`.** The drafted option "territories as host config" is **wrong on the evidence**: territories has three importers, and they land on *both* sides of the split — `main.ts` and `mcp/server.ts` (host) and `governor/wiring/wiring.ts` (provider, the review pane). Host-held config cannot serve a provider consumer, so the only alternative to publishing is two copies — which is precisely the failure `territories.ts`'s own header names: *"a prefix present in the pane's list but missing from capture's means the pane politely skips a folder while capture quietly writes its note bodies to disk."* On this operator's vault the guarded prefixes include `80-89` (legal/PII, standing rule that its contents do not leave it), so a drift here is a PII leak into capture blobs, not a tidiness problem. One list, two consumers, one published contract.

   Net: four boundary edges retire on this ruling alone (`server.ts`→observations, `server.ts`→territories, `main.ts`→territories, and `observations/local-store.ts`'s two back-edges leaving the governor tree entirely). Unchanged by this ruling: the hardcoded prefix list itself is still this vault's folder names, which issue #321 tracks promoting to real configuration — publishing the module does not fix that, and must not be mistaken for having fixed it.

**DEFERRED to `reference/hostile-threat-model`** — hostile local code is their only justification:

- **Condition 4's per-hook byte copies.** The cheap half survives (deep-freeze `operation` and `actor`, which also catches a BUGGY hook mutating shared state); copying every write's bytes per hook is a per-write cost paid against an actor §0 excludes.
- **Condition 8 as a perimeter rule.** `registerWriteVeto` still ships only when something needs it — for the ordinary reason that unused API surface is dead code, not as a safety property.
- **Condition 1's runtime transitive-walk test.** The WeakMap placement stays (free); walking `app` to prove no hook is reachable is adversary-shaped. The source tripwire suffices.

**S2's exit criterion is NOT the crossing count.** Of S1's 45 host→governor edges, 38 disappear only at S3 — when the code physically moves, not because a hook exists. S2 succeeds when the seam is proven sufficient for the hook-shaped crossings and the six rules have their pins; the concrete target is **`ServerCtx` naming zero provider types** (edges 34–38 of the review's disposition table) plus the `server.ts` propose block retired into the observer.

## 6. Module inventory — disposition of everything in the bundle today

| Module / area | D07 tier | Disposition | Notes |
|---|---|---|---|
| Socket transport + bridge | public core | **host — EXECUTED (S3c)** | unchanged in behavior; the machine-local dir reverts with the id, so the bridge, socket and discovery live in `~/.claude/vault-mcp/` again, with a grace-period bridge and a `legacy: true` discovery copy left in `~/.claude/governor/` so existing `claude mcp` registrations keep working with no re-registration |
| Guard (read-only, allowlist), kernel v0 (queue, journal, idempotency, locks, uid, record guard) | public core | **host — EXECUTED (S3c)** | unchanged; record-guard stays host-side (it guards `record: true` notes, not acceptance) |
| Operation executor + action registry + surface inventory | public core | **host — EXECUTED (S3c)** | the propose hook generalized into the seam observer at S2, as planned |
| Accepted-family frontmatter floor (`@vault-mcp/core` accept guard) | public core | **host keeps the floor — EXECUTED (S3c)**; the fuller transition rules are the provider's | defense in depth: even a governance-less host refuses agents stamping `accepted`. Note the draft said the provider "registers the fuller transition veto" — it does not, because `registerWriteVeto` was never built (condition 8's YAGNI-at-a-perimeter ruling); the fuller guard is host-side in `@vault-mcp/core` and the legacy-writer guard is provider-internal |
| Core obsidian tools (read/write/search/links/uid/scheme-addressing/locks/…) | public core / optional | **host — EXECUTED (S3c)** | unchanged |
| External-tool registry + `vault-mcp-api` | public core | **host — EXECUTED (S3c)** | The SDK gained the seam's public contract (`WriteFacts`, `SeamRefusal`, `GovernanceSeam`, `JournalActor`, mirrored from the host) plus `registerGovernance(plugin, hooks)` beside `publishTools`, and `HOST_PLUGIN_IDS` became `["vault-mcp", "governor"]`, current host first. **`apiVersion` stayed 1**, against this row's draft: the methods are additive, and bumping it would make every already-published SDK build refuse to register against a newer host. Capability discovery and the caller-scope envelope remain the apiVersion-2 items |
| Proposals, verification, admission, cohorts, standing chain, resolver | public core (governed) | **governor provider — EXECUTED (S3c)** | perimeter internals unchanged, as planned |
| Mandates, transformations, promotion (WP9/WP10) | public core (governed) | **governor provider — EXECUTED (S3c)** | unchanged |
| Sessions + origins | governed | **split, per condition 7 — EXECUTED (S3c)** | The host mints, keeps a lifecycle log (`opened`/`closed`/`expired`) at `<host plugin dir>/sessions.jsonl`, and applies its own pure expiry floor, which needs no store and no provider. The provider keeps REFUSAL state only — revocation and session→mandate attachment — in its existing `governance/sessions.jsonl`, and answers the seam's refusal hook from it. `SessionV1` is a published contract in `@vault-mcp/core`, not provider-internal. Condition 7 said the seam needs no connection-lifecycle notification and none was added; the cost of that, stated rather than buried, is that the provider no longer witnesses session OPEN, so `revoke` and `attachMandate` now act on a session id with no prior `opened` record required. A deliberate loosening, safe in this direction because revocation can only add refusals and because mandate fit already binds by session id rather than by the session record |
| Observation capture (blob store) | governed → **reclassified public core** | **host**, per condition 9's S3 ruling — **EXECUTED (S3c)** | one consumer (`mcp/server.ts`), already built on host store + host paths; no capture hook was added. The blobs live under the host's own machine-local dir, `~/.claude/vault-mcp/observations/` |
| Observation *references* (`{id, digest, capture}`, durability rules, D16 ephemerality) | governed | **governor provider — EXECUTED (S3c)** | contract-level reasoning in `subject-v1.ts` + admission/mandate policy; the provider never sees the store |
| Guarded territories (`EXCLUDED_PREFIXES` / `isExcludedTerritory`) | **published contract** | **`@vault-mcp/core` — EXECUTED** | three importers on BOTH sides; two copies would be a PII leak, not a style nit (see condition 9's ruling). Issue #321 still owns making the list configurable |
| History store + migration/cutover/binding | governed | **governor provider — EXECUTED (S3c)** | the machine-local history repository stays exactly where it is, at `~/.claude/governor/history/<vault-slug>/`, because the provider keeps the `governor` id — zero moves, and backups unchanged. That is the point of assigning the old id to the provider rather than to the host |
| Review pane, gesture perimeter, dispositions | governed | **governor provider — EXECUTED (S3c)** | the tripwire tests moved with it |
| Legacy acceptance (baselines, queue, retired writers) | governed, retired-era | **governor provider — EXECUTED (S3c)** | rollback fidelity lives where the cutover lives |
| `obsidian_pending_review`, `governance_revisions`, `governance_submit_revision`, mandate draft/list | governed | **governor provider — EXECUTED (S3c)**, published via `vault-mcp-api` | Dogfoods the publishing path, and all five keep their exact shipped spellings through the host's closed `GRANDFATHERED_TOOL_NAMES` table — the one carve-out from F1's `obsidian_*` refusal and from the `<sanitized owner id>_<bare name>` rule, gated on the owner id the table names rather than on whoever holds a seam registration, and closed so that a later tool takes the ordinary form. This row's draft said "read-only claims trusted via the existing per-plugin trust list", which had the default backwards: an external tool's read-only claim is DISTRUSTED unless the operator lists `governor` in `trustedReadOnlyPlugins`, and under an active path allowlist F3 refuses outright the four that carry no recognized path key, leaving only `governance_submit_revision` (which takes `path`) scoped per-path. Stricter than the folded arrangement, and the four also lose their in-tool visibility filtering to buy it — a satellite cannot reach the host's guard settings |
| Scheme / scope provider (JD) | public optional | **HOST — ruled 2026-09-05 at S8, overturning this row's draft** | The drafted "satellite" did not survive the code, the observations-ruling precedent applied a third time. Scheme addressing (`jd:<address>`) is wired into the host's guard interception point (`mcp/guarded.ts`) exactly like `uid:` addressing — every path argument of every tool, host or external, can carry a scheme ref, and the resolution must run before the allowlist checks the resolved path. That makes it host CAPABILITY, the same category as the uid index, not a capability pack. Three further host consumers seal it: the registry shapes `ServerCtx` settings, and the conformance rail's drift/debt sources build from scheme configs. What keeps this honest for third parties: the provider grammar is already config-pluggable inside the host (`SchemeInstanceConfig`, per-provider validation), and if an external scheme ever needs in, the shape is an address-resolver hook on the api — an apiVersion-2 item beside the partial-result envelope and caller-scope items, not something to invent while only the host consumes it. |
| Vocabulary provider | public optional | **satellite — EXECUTED** | read-only; its KERNEL went to `@vault-mcp/core`, not into the satellite — conformance is its second consumer |
| Health scan | public optional | **satellite — EXECUTED** | read-only; absent from this table until S7, which is how it stayed unplanned while everything around it was scheduled |
| Conformance | public optional | **satellite** | read-only; the one remaining consumer of the vocabulary kernel inside the host |
| Bases | public optional | **satellite — EXECUTED** | read-only; the capture seam moved with it, whole and uncopied |
| Fileclass CLI proxy | public optional | **satellite — EXECUTED** | already doubly-gated |
| Provenance (inspect + slot audit) | optional / private regen | **satellite — EXECUTED** | formerly governor-lead's #257 lane; lane consolidated 2026-08-25 |
| JD scaffolding | private operator | **satellite — EXECUTED** | absent from this table until the mutating tier, the same way the health scan was absent until S7 — the second time a capability stayed unplanned because nobody had written its row |
| Skills compiler | private operator | **satellite — EXECUTED** | the biggest single extraction; least entangled |
| Cross-session | private operator | **satellite — EXECUTED** | |
| Triage | private operator | **satellite — EXECUTED** | |
| QuickAdd compile | private operator | **satellite — EXECUTED** | the pilot, extracted ahead of S1 |
| Apple-notes importer glue | private operator | **satellite** (or fold into an existing operator satellite) | |

Retirements: none proposed here — WP10c already did the era's retirement; the split moves code, it does not re-decide authority.

## 7. Extraction order — the work packages

Each package: one PR, tests move with their code, the full suite green at every step, the perimeter tripwires never leave the governance package, and status-and-compatibility gains a per-plugin release-state row as plugins gain independent releases.

- **S1 — rename and restructure, zero behavior change.** Repo rename; `packages/plugin` → `packages/host` + `packages/governor` as *directories* while still building ONE plugin artifact (imports moved, boundaries made visible, no runtime change). The point: make every later diff small and reviewable.
- **S2 — the seam, consumed in-tree.** Build the hook API in the host layer; the governance layer registers its vetoes/observers through it *while still compiled into one plugin*. Proves the seam's sufficiency with zero distribution risk. The seam's five rules get their pins here.
- **S3 — the actual split.** Two plugin artifacts; id migrations (`governor` → `vault-mcp` for the host; the provider takes `governor`); the governance-state migration map executes; the runbook gains the two-plugin install/upgrade story. This is the package with live-vault stakes — it lands with a rollback path and an independent release-readiness review. **S3c EXECUTED: `packages/plugin` became `packages/host` (id `vault-mcp`, "Vault MCP") and `packages/governor` (id `governor`, "Governor"), both 0.19.0 — built, and NOT yet cut over on the operator's vault (`docs/status-and-compatibility.md` stays the owner of shipped truth, and `docs/s3c-migration-plan.md` is the cutover procedure).** Six findings worth carrying.

  **First, five tool names were grandfathered, and the table that does it is CLOSED.** The publishing rule is `<sanitized owner id>_<bare name>` and the host refuses any published name beginning `obsidian_` (F1), so under it `governance_revisions` would have become `governor_governance_revisions` and `obsidian_pending_review` could not have been published at all. `GRANDFATHERED_TOOL_NAMES` in `packages/host/src/mcp/external-tools.ts` maps each of the five shipped spellings — `obsidian_pending_review`, `governance_revisions`, `governance_submit_revision`, `governance_mandate_draft`, `governance_mandates` — to the single owner id allowed to publish it. Two shape decisions are load-bearing. It is host-held rather than a publisher-declared `exactName: true` flag, because a flag would let any plugin claim any name including one in the reserved namespace. And it is gated on the id the table names, not on `seam.providerIds()`, because gating on "holds a seam registration" would let anything that registers a write observer claim `obsidian_pending_review` — strictly worse. It is closed: a later tool in that package takes the ordinary namespaced form. Every other satellite in the suite paid the rename tax deliberately, and the seven `obsidian_jd_*` renames were recorded as a BOUNDARY rather than a choice; these five could not pay it only because they were already on the wire when the split happened.

  **Second, their allowlist posture got STRICTER, the same finding class as S4–S8.** As external tools their `readOnlyHint: true` is distrusted unless the operator lists `governor` in `trustedReadOnlyPlugins`, and under an active path allowlist F3 refuses any external tool whose arguments carry no recognized path key — which is four of the five. Only `governance_submit_revision` takes `path` and stays scoped per-path. The four also lose their in-tool visibility filtering, because a satellite cannot reach the host's guard settings; what replaces it is the host's gate refusing the whole call rather than trimming the answer. Under an allowlist that is stricter and less available at once, which is the honest shape of it; with no allowlist configured, which is this operator's case, the filters were vacuous and nothing changes.

  **Third, the session split shipped exactly as condition 7 specified, and the loosening it implies is recorded rather than glossed.** The host mints, keeps a lifecycle log (`opened`/`closed`/`expired`) at `<host plugin dir>/sessions.jsonl`, and applies its own pure expiry floor, which needs no store and no provider. The provider keeps refusal state only — revocation and session→mandate attachment — in its existing `governance/sessions.jsonl`. **No connection-lifecycle notification was added to the seam**, which condition 7 said the provider does not need because it never mints. The consequence is that the provider no longer witnesses session OPEN, so `revoke` and `attachMandate` now operate on a session id without requiring a prior `opened` record. That is a real loosening, deliberately taken: revocation can only ADD refusals, and mandate fit already binds by session id rather than by the session record, which the provider's own mandate wiring already calls provenance rather than a gate.

  **Fourth, the journal is ADOPTED BY COPY, never by move.** The provider keeps `.obsidian/plugins/governor/` and everything under `governance/` — zero moves for the authority state, which is the whole reason the provider took the old id rather than the host. The host installs into `.obsidian/plugins/vault-mcp/` and on first load copies three things out of the provider's folder: `journal/`, `install-id.json`, and the host-owned keys of `data.json`. Copy, not move, and nothing is deleted: a half-completed adoption must leave a rollback to the single 0.18.2 artifact intact, and an append-only record is exactly the thing that must not be in flight during a folder rename. The cost is a period where two copies of the journal exist on disk and only one of them is being appended to — visible, bounded, and preferable to a move that can fail halfway through history.

  **Fifth, the client-visible name did NOT follow the plugin id.** The Claude Code MCP server stays registered as `governor` and the tool prefixes stay `mcp__governor__*`, so no operator re-registers anything. The plugin id is an Obsidian-internal identifier; the client-visible name is the tool prefix, and renaming shipped tool prefixes breaks every live agent session for zero semantic gain — the same standing rule that grandfathered the five tool names above.

  **Sixth, the `~/.claude` dir reversal is asymmetric on purpose.** `~/.claude/vault-mcp/` is the host's primary state dir again (bridge, socket, discovery, `observations/`), with a grace-period bridge and a `legacy: true` discovery copy left at `~/.claude/governor/` so existing registrations keep resolving — the exact machinery the 0.12.0 migration ran in the other direction. But the provider's history repository stays at `~/.claude/governor/history/<vault-slug>/` and does not move, because it belongs to the plugin that kept the id. So one `~/.claude` dir now holds live state for both plugins, which reads oddly and is correct.
- **S4…Sn — satellites, private tier first** (skills, cross-session, triage — the biggest "arcane" contributors, least entangled), then the optional tier (bases, vocab, conformance, scheme, fileclass, provenance — of which bases, vocab and health went together at S7 as the read tier, and fileclass, provenance and JD scaffolding went together as the mutating tier immediately after, leaving conformance and scheme). **Exception, executed (Nelson, 2026-08-26): QuickAdd choices extracted FIRST, ahead of S1 — the publishing API already ships, so the pilot satellite proves the path and surfaces apiVersion-2 gaps before the seam is designed.** First gap found: the external-tool contract cannot express a partial-result error envelope (structured data + isError) — an apiVersion-2 item. Each satellite dogfoods `vault-mcp-api` and shrinks the host. **S4 executed: the skills compiler has landed as the `packages/skills` satellite (plugin id `vault-skills`), the first private-tier extraction after the QuickAdd pilot.** It produced one finding worth recording: the host's external-tool gate turned out to be strictly stricter than the module's own in-tool allowlist filtering — a mutating external tool with no path-key argument is now blocked outright under an allowlist, where the in-module filter would previously have let it through unscoped — so the extraction tightened the boundary rather than loosening it. `isVisible` and `GuardSettings` were published into `@vault-mcp/core` for both sides to share, rather than copied into the satellite. **S5 executed: triage landed as the `packages/triage` satellite (plugin id `vault-triage`).** The tool names changed to `vault_triage_*` because the plugin id is the tool namespace (`triage_queue` → `vault_triage_queue`, `triage_dispose` → `vault_triage_dispose` — unlike skills, `vault-triage` does not sanitize to a prefix the bare tool names already carried). The bases capture seam (`queryBaseRows`, which drives a hidden Bases leaf guarded by a module-scoped serializer) deliberately did not come with triage — it was neither moved nor copied — so base-backed queues (`triage_queue {base}` / `{view}` / `{queue}`) now refuse typed `bases_unavailable`; the inbox-marker queue is unaffected, and evaluated Base rows are available through the bases surface's own query tool instead. (At the time that meant the host's `base_query`; at S7 the seam and the tool moved together into the `vault-bases` satellite as `vault_bases_query`, which reinforces the reasoning rather than undoing it — the serializer still exists exactly once, and a copy in `packages/triage` would still race it.) **S6 executed: cross-session coordination landed as the `packages/crosssession` satellite (plugin id `vault-crosssession`), completing the private tier.** The four tool names changed to `vault_crosssession_*` for the same plugin-id-is-the-namespace reason triage's did. Two findings worth recording. First, the guard-argument question came out the OPPOSITE way from triage's: triage renamed `target` → `target_path` to give the host's guard something to scope, but cross-session's `channel` was deliberately left un-path-keyed, because it is a REF (uid | folder-note path | folder) rather than a path, because the file `post` writes is discovered inside the handler and named by no argument, and because path-keying it would refuse every uid-addressed call under an allowlist — the bug the scheme-write `to` → `to_address` rename fixed, in reverse. The consequence, accepted and documented, is that all four tools are blocked wholesale under an active allowlist. Second, this extraction was the first to move LIVE OPERATIONAL STATE rather than only configuration: the per-handle read receipts (`crosssession-receipts.json`) lived in the host's plugin directory, and losing them would have made every affected handle re-read entries it had already attested — so the satellite adopts that file once by merge, under a latch separate from the config latch, and its store has no write path that can touch the host's copy. The host's `RECORD_EXEMPT_OPS` was re-examined and deliberately NOT widened: `post` was already inside the kernel as a module tool and is still unreachable by the record check on arguments, so the extraction changed nothing there. **S7 executed: the READ TIER landed as three satellites in one package — `packages/vocab` (`vault-vocab`), `packages/health` (`vault-health`) and `packages/bases` (`vault-bases`) — taking the host's built-in capability modules from eight to five.** All eight tool names changed for the plugin-id-is-the-namespace reason, with the redundant owner prefixes stripped rather than carried into a second namespace: `obsidian_vocabularies` and its three siblings became `vault_vocab_*`, `obsidian_health`/`obsidian_lint` became `vault_health_scan`/`vault_health_lint`, and `base_list`/`base_query` became `vault_bases_list`/`vault_bases_query` (keeping `base_` would have published `vault_bases_base_query`). Four findings worth recording. **First, the bases capture seam moved WITH bases, which inverts S5's decision without contradicting it.** S5 cut triage's base-backed queues loose rather than take `queryBaseRows` along, because the capture drives a hidden Bases leaf — a global resource a module-scoped serializer holds to one capture at a time — and a second serializer in a second plugin would race the first. That never applied to bases itself: bases owns the leaf and the serializer, and once triage was gone `base_query`'s own handler was the seam's only production caller. So the whole set moved as one piece with nothing copied, and the mirror-image risk (a host copy racing the satellite's) is precisely why it had to be a move. The race argument is reinforced, not overturned. **Second, the vocabulary KERNEL went to `@vault-mcp/core`, not into the satellite.** It has two consumers and always did — the four tools, and the host's own conformance rail (`conformance/packs/vocab.ts` wraps `noteVocabFindings`; `conformance/cli.ts` builds a `VocabRegistry` per run; `snapshot.ts` and `rule-pack.ts` are typed over `VocabNote`). That is the same shape as the `queryBaseRows` question with the opposite resolution available, because unlike the Bases leaf a rule core has no global resource to race — only a correctness hazard if forked, since two copies of a vocabulary rule core is how one vault gets two vocabularies. Published on the `isVisible` (S4) and `executeQuickAddChoice` (S5) precedent; conformance is itself a planned satellite, so core is where both consumers were always going to meet. **Third, `resolveScope` was published into core for the same reason,** because `obsidian_lint` hand-guards a bare `scope` argument that is not a path key and so is invisible to `guardCall` — a gap that was a live read-boundary bypass until 2026-08-29. Its two callers now sit in two plugins, and the single copy gained one refusal in the move (a scope containing a backslash is refused `invalid_scope`), which made the host's `obsidian_check_links` and the satellite's lint stricter in the same motion. **Fourth, issue #381's list shrank.** It named `obsidian_health`, `provenance_reconcile` and `obsidian_conformance_debt` as whole-vault readers appearing in no enumerated exception list, and asked for enumerate-or-filter. For the health pair the question is moot: as untrusted external tools carrying no recognized path-key argument, both are now blocked wholesale while a path allowlist is active — stricter than the documented-exception outcome the issue was weighing — so two remain, both still in the host. The allowlist posture across the eight tools is deliberately NOT uniform and is documented per tool in each package, because the host's gate tests the arguments a call actually carries: `vault_vocab_resolve_term` is blocked when called with `{token}` and scoped when called with `{path}`. **The MUTATING TIER executed next, and it was the last extractable tier: `packages/fileclass` (`vault-fileclass`), `packages/provenance` (`vault-provenance`) and `packages/jd-scaffold` (`vault-jd-scaffold`), taking the host's built-in capability modules from five to TWO — `scheme` and `acceptance`.** All eighteen tool names changed, again for the plugin-id-is-the-namespace reason and again with redundant owner prefixes stripped (`fileclass_set_where` → `vault_fileclass_set_where`, `provenance_check` → `vault_provenance_check`, `obsidian_jd_standard_zeros` → `vault_jd_scaffold_standard_zeros`). For the seven `obsidian_jd_*` tools the rename was FORCED rather than chosen: the host's external-tool registration refuses a published name beginning `obsidian_`, so no plugin id could have reproduced those spellings. Five findings worth recording. **First, the allowlist posture is UNIFORM across the tier, and deliberately unlike the bases asymmetry.** No tool in the three surfaces carries an argument the host recognizes as a path key: the note argument is spelled `note_path`, not `path`, on all six tools that name a note. So the host's F3 gate blocks each surface wholesale while a path allowlist is active. For fileclass that reproduces exactly what the module did (it refused its own whole surface, because the CLI runs its engine over the whole vault and its output cannot be attributed to paths). For provenance and JD scaffolding it is strictly stricter than the module was, and that is the point: keeping `path` would have handed the guard one argument while the work reached further — provenance's freshness answer names every path the checked note's `derived-from` globs resolve to, JD promote-to-folder writes to destinations the plan COMPUTES and no argument names, and JD reindex reads every sibling index file vault-wide at the area and system tiers. Scoping the argument while the blast radius exceeds it is the illusion of a check, which is the cross-session `channel` reasoning applied to arguments that genuinely ARE paths. The cost is real and is documented in each package: under an allowlist these surfaces are unavailable rather than partially available, and the reversal is one word per tool. **And the cost is not only availability — the ledger has a second half that is easy to omit and must not be.** `collectPaths` is not the allowlist's private walker: the same list feeds record immutability (a mutating op that NAMES a `record: true` note is refused at dequeue), the advisory-lock consult, and the journal record's argument-derived `target.path`. With no argument in `PATH_KEYS` all three see an empty path list on every call in the tier, so a `record: true` note is no longer protected from `vault_fileclass_set` or `vault_jd_scaffold_reindex_category` by that kernel check, a foreign scope claim covering the note is no longer disclosed, and the journal names no target path except where a handler reports `filesChanged`/`files` as effects. NONE of those three is gated on an allowlist, so they are lost in the ordinary no-allowlist case too — the opposite direction from the tightening, and the strongest argument anyone will have for reversing the rename. **Second, issue #381 is down to one tool.** It named `obsidian_health`, `provenance_reconcile` and `obsidian_conformance_debt`; the health pair went at S7, `provenance_reconcile` went here on identical terms, and `obsidian_conformance_debt` alone remains in the host and still owes the enumerate-or-filter decision. Note precisely what changed: the reconcile scan still reads the whole vault, and would still do so with no allowlist configured — what it no longer does is return a whole-vault answer to a session the host has scoped. **Third, the module host's `mutating: true` escape hatch is now DORMANT.** These three were the only modules that ever declared it, so `mutatingModules` is empty and the gate reduces to its original rule: a module tool must declare `readOnlyHint: true`. The flag, the gate branch and their tests were KEPT rather than deleted — unlike the seam's `registerWriteVeto`, this is not perimeter surface that was never used; it is a documented capability of the module host with six shipped users behind it over the split's life (skills, triage and cross-session before these three), and a module needing it again declares it the same way they did. **Fourth, four more small pieces were published into `@vault-mcp/core` rather than forked:** `spawnEnv`, `findBinary` and `findObsidianBinary` (the fileclass satellite spawns a CLI and must augment PATH and locate the obsidian binary exactly as the host does — a drifted copy would fail only on some machines, silently), and `scanForAcceptFence` (whose two callers, the host's `obsidian_cli` template guard and the JD scaffold template apply, now live in different plugins; two copies of an accept predicate is how one vault gets two definitions of "accepted"). The `scanForAcceptFence` move is behaviour-preserving by construction rather than by inspection: every symbol it is defined over was already core's, reached from the host through a bare re-export. **Fifth, the apiVersion-2 gap the QuickAdd pilot found has now bitten a second surface.** The pilot recorded that the external-tool contract cannot express a partial-result error envelope (structured data plus `isError`). The fileclass module used exactly that — the host's `okError` — so a failed CLI run kept its structured per-command report instead of being flattened to text. The boundary has no equivalent, and throwing would destroy the report, so the satellite RETURNS the report with an explicit `succeeded: false`: a CLI failure now arrives as a successful MCP call carrying a report that says it failed. That is the honest trade, it is stated in the tool descriptions and pinned by a test, and it is the second independent argument for the apiVersion-2 item. **[ACTED ON IN TWO ROUNDS, both 2026-09-07 — and the finding above stands as the decision record, unedited, because it is the argument that made the first round correct.**

**Round 1: `note_path` went onto the host's `PATH_KEYS`.** The three kernel checks the paragraph above names are LIVE again for every argument-named note, and an active allowlist scopes those tools per-path; tools naming no note stay pathless and keep F3's refuse-all.

**Round 2, after an independent review: the fix was narrowed, because round 1 had reached further than its reason.** It also made three READS — `vault_fileclass_explain`, `vault_fileclass_get`, `vault_provenance_check` — per-path scopable, which is exactly the weaker posture the "first finding" above rejects, re-created silently. The rule that settles it, and that binds any satellite adding a note-taking tool: **kernel visibility is a MUTATING concern.** The record guard, the lock consult and the journal target all bind at the mutating dequeue, so a read gains nothing from being path-keyed and loses only the refusal. So those three reads spell their argument `note`, which the host does not recognize, and are refused outright under an allowlist; `vault_fileclass_set`, `vault_jd_scaffold_promote_to_folder` and `vault_jd_scaffold_reindex_category` keep `note_path` and are scoped per-path with the kernel watching the note they rewrite.

**What that leaves, stated as a residual rather than as a resolution.** `vault_jd_scaffold_reindex_category` is mutating AND reads vault-wide: under an allowlist it now proceeds, scoped to the index note it writes, while its area-management and system tiers read every sibling `XX.00` file — hidden ones included — and can fold their names into the visible note's `## Contents`. **This was weighed and ACCEPTED, not overlooked.** Accepted because the kernel protection it buys is live on every vault regardless of allowlist, while the leak requires an allowlist and the operator's allowlist is empty; and because the reversal is one word (`note_path` → `note` on that tool), which restores F3's refuse-all at the cost of the record guard on the note it rewrites. The proper close is the apiVersion-2 item — an SDK that carries the caller's scope to a publisher — which wakes the sibling filter still sitting dormant in that handler. `vault_jd_scaffold_promote_to_folder`'s COMPUTED destinations are a different and already-documented class: discovered writes the argument-derived guard never sees, the `obsidian_repoint_link` boundary, mitigated by the `filesChanged`/`files` effects report.

**Net posture, per surface, so nobody has to reconstruct it:** fileclass — seven refused, `set` scoped. Provenance — all three refused. JD scaffold — five refused, two scoped, one ratified residual.]**
- **Then: the Gate 2 evidence checkpoint** (§657–664), run on the split build — the architecture we intend to keep, per Nelson's sequencing ruling.

## 8. What does not change

The admission perimeter's internals and doctrine. The standing chain's format and location. The cutover state, the store binding, both backup repos. WP9/WP10 semantics. The register's D01–D18 substance — except the two entries the adoption ruling records:

- **D14 (reinterpreted):** Gate 3's "final public/private bundle split" is realized as the plugin suite split, resequenced ahead of the Gate 2 evidence checkpoint. (Nelson, 2026-08-25.)
- **D07 (realized):** the tier boundaries become plugin boundaries.

## 9. Risks and costs, named

- **The seam is new perimeter surface.** It is refuse-or-observe only, but it is the one place a design error could leak capability — hence S2's in-tree proving step and the independent perimeter review before S3.
- **Release multiplication.** One release lane becomes several manifests. The lockfile-regen gap already flagged in the release pipeline gets worse before it gets better; the S3 package must include per-plugin release tooling, not inherit the gap.
- **Id migration on the live vault** (§4). Shipped machinery, real click.
- **Test-suite split.** 4,000+ tests partition along package lines; the cross-package integration tests (seam round-trips, publish round-trips) are NEW tests S2/S3 must add, not inherited coverage.
- **Governance becomes removable (perimeter review finding 3).** Today `obsidian_plugin_toggle` refuses the host's own id for self-preservation, and disabling governance is impossible because it IS the host. Post-split, `governor` is just another plugin id — one agent-reachable call, or one `app.plugins.disablePlugin` from a renderer script, and the perimeter is gone, with the host unable to tell removal from never-installed (all governance state migrates to the provider per §4). Condition 6 answers it with a host-side latch. Named here because it is the split's biggest doctrinal cost and it was absent from the first draft of this list.
- **Docs corpus restructure.** The corpus stays one suite-level set with per-plugin ownership marked; status-and-compatibility grows a per-plugin table. A separate small package inside S3.
- **The master brake (governor-lead's post-#359 observation, carried here).** Today "stop all automatic admission" means revoking each mandate one at a time — and a human reaching for a brake under stress wants ONE control. The provider should grow a single gesture-gated "suspend automatic admission" switch: doctrine-clean because it only refuses MORE (a suspended state refuses the mandate door outright, mandates untouched, resumable by the same gesture class). Designed and built inside the provider during S2/S3, not bolted on after.

## 10. Open decisions (Nelson)

1. **The repo name.** DECIDED: `obsidian-mcp-suite` (see §4).
2. **Adopt this document** — which records the two register entries in §8 and green-lights S1.
3. **Satellite release posture:** released/versioned per satellite from day one, or private-tier satellites stay unreleased (installed from the monorepo build) until someone needs them independently? Recommendation: the latter — release ceremony only where there is an audience.
